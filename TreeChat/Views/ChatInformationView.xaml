<UserControl x:Class="TreeChat.Views.ChatInformationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TreeChat.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="0,8,8,8">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 消息展示区卡片 -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="12" 
                BorderBrush="#E2E6EA" BorderThickness="1" Effect="{StaticResource CardShadow}">
            <ScrollViewer x:Name="MessageScrollViewer" 
                          VerticalScrollBarVisibility="Auto" 
                          HorizontalScrollBarVisibility="Disabled">
                <StackPanel>
                    <StackPanel Margin="0,0,0,16">
                        <TextBlock Text="提问" FontSize="12" Foreground="#6C7A8A" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <TextBlock Text="{Binding UserMessage}" 
                                   TextWrapping="Wrap" FontSize="14" Foreground="#1F2A3A"/>
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="回答" FontSize="12" Foreground="#6C7A8A" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <TextBlock Text="{Binding AIReply}" 
                                   TextWrapping="Wrap" FontSize="14" Foreground="#1F2A3A"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- 输入区卡片 -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Margin="0,12,0,0"
                BorderBrush="#E2E6EA" BorderThickness="1" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding InputMessage, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         MinHeight="60" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
                <Button Grid.Column="1" Content="发送" Command="{Binding SendMessage}" Width="70" Height="36" Margin="12,0,0,0"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>