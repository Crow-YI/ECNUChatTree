<UserControl x:Class="TreeChat.Views.ChatManagementPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="White" CornerRadius="8" Margin="0,8,8,8" Padding="10" 
            BorderBrush="#E2E6EA" BorderThickness="1" Effect="{StaticResource CardShadow}">
        <StackPanel>
            <Button Content="新对话" Command="{Binding CreateNewChat}" Margin="0,0,0,8"/>
            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="保存" Command="{Binding SaveChat}"/>
                <Button Grid.Column="2" Content="读取" Command="{Binding LoadChat}"/>
            </Grid>
            <Button Content="重命名" Command="{Binding RenameChat}" Margin="0,0,0,8"/>

            <ListBox ItemsSource="{Binding ChatList}" 
                     SelectedItem="{Binding SelectedChat, Mode=TwoWay}"
                     BorderThickness="0" Background="Transparent">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding TreeTitle}" Margin="5,2" FontSize="13"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Border>
</UserControl>